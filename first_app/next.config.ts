// next.config.ts (Single export - no mixing)
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // allowedDevOrigins: ['192.168.100.130'],
   allowedDevOrigins: ['192.168.0.103']
  
};

export default nextConfig;